import { render, screen, waitFor } from "@testing-library/react";
import AdminContent from "../../../../components/admin/courses/content"

describe("testing admin content component", () => {
  beforeEach(async () => {<AdminContent contentOpen={undefined} openContent={false} handleContentClose={undefined} handleAddVideoOpen={undefined} handleAddArticleOpen={undefined}/>});
  test("", () => {});
});
