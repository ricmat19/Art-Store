import { render, screen, waitFor } from "@testing-library/react";
import AdminUpdateCourse from "../../../../components/admin/courses/updateCourse";

describe("testing admin update course component", () => {
  beforeEach(async () => {<AdminUpdateCourse/>});
  test("", () => {});
});
