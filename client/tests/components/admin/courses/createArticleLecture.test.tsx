import { render, screen, waitFor } from "@testing-library/react";
import AdminArticleLecture from "../../../../components/admin/courses/createArticleLectureModal";

describe("testing admin create article lecture component", () => {
  beforeEach(async () => {<AdminArticleLecture open={undefined} handleClose={undefined} lecture={undefined} section={""} id={""}/>});
  test("", () => {});
});
