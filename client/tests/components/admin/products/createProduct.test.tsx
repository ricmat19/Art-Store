import { render, screen, waitFor } from "@testing-library/react";
import AdminCreateProduct from "../../../../components/admin/products/createProduct";

describe("testing admin create product component", () => {
  beforeEach(async () => {<AdminCreateProduct open={undefined} handleClose={undefined}/>});
  test("", () => {});
});
