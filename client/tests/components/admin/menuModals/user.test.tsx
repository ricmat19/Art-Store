import { render, screen, waitFor } from "@testing-library/react";
import AdminUserMenuModal from "../../../../components/admin/courses/content";

describe("testing admin user component", () => {
  beforeEach(async () => {<AdminUserMenuModal contentOpen={undefined} openContent={false} handleContentClose={undefined} handleAddVideoOpen={undefined} handleAddArticleOpen={undefined}/>});
  test("", () => {});
});
