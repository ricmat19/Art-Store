import { render, screen, waitFor } from "@testing-library/react";
import AdminMainNav from "../../../components/admin/mainNav";

describe("testing admin main nav component", () => {
  beforeEach(async () => {<AdminMainNav/>});
  test("", () => {});
});
