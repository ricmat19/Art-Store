import { render, screen, waitFor } from "@testing-library/react";
import AdminDay from "../../../../components/admin/events/dayModal";

describe("testing admin day component", () => {
  beforeEach(async () => {<AdminDay date={undefined} handleClose={undefined} open={undefined} dateEvents={undefined} displayDeleteModal={false}/>});
  test("", () => {});
});
