import { render, screen, waitFor } from "@testing-library/react";
import AdminCreateEvent from "../../../../components/admin/events/createEvent";

describe("testing admin create event component", () => {
  beforeEach(async () => {
    <AdminCreateEvent />;
  });
  test("", () => {});
});
