import { render, screen, waitFor } from "@testing-library/react";
import AdminPagesNav from "../../../components/admin/pagesNav";

describe("testing admin pages nav component", () => {
  beforeEach(async () => {<AdminPagesNav/>});
  test("", () => {});
});
