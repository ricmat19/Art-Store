import { render, screen, waitFor } from "@testing-library/react";

describe("testing user add to cart component", () => {
  beforeEach(async () => {});
  test("check that the add to cart modal disappears upon clicking outside of the modal", () => {});

  test("check that the add to cart modal remains upon clicking inside of the modal", () => {});

  test("check the correct products title is displayed in the modal", () => {});

  test("check the text 'has been added to your cart.' is displayed if the selected product was just added to the cart", () => {});

  test("check the text 'has already been added to your cart.' is displayed if the selected product is already in the cart", () => {});

  test("check that the add to cart modal remains upon clicking within the modal", () => {});

  test("check that the view cart link directs you to the cart page", () => {});

  test("check that the continue shopping link directs you to the store page", () => {});
});
