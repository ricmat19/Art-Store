import { render, screen, waitFor } from "@testing-library/react";

describe("testing user products nav component", () => {
  beforeEach(async () => {});
  test("check that products categories are mapped in nav", () => {});

  test("check that the products nav prints link directs you to the prints page", () => {});

});
