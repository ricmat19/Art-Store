import { render, screen, waitFor } from "@testing-library/react";

describe("testing user links component", () => {
  beforeEach(async () => {});
  test("check links submit properly stores the users website address", () => {});

  test("check links submit properly stores the users twitter address", () => {});

  test("check links submit properly stores the users linkedin address", () => {});

  test("check links submit properly stores the users youtube address", () => {});
});
