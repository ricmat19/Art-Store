import { render, screen, waitFor } from "@testing-library/react";

describe("testing user bio component", () => {
  beforeEach(async () => {});
  test("check that the bio tab displays the profile image upon selection", () => {});

  test("check bio submit properly stores the user's profile image", () => {});

  test("check bio submit properly stores the user's bio", () => {});
});
