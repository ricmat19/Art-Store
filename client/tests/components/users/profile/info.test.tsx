import { render, screen, waitFor } from "@testing-library/react";

describe("testing user info component", () => {
  beforeEach(async () => {});

  test("check info submit properly stores the users first name", () => { });

  test("check info submit properly stores the users last name", () => { });

  test("check info submit properly stores the users email", () => { });

  test("check info submit properly stores the users phone number", () => {});

  test("check info submit properly stores the users address", () => {});

  test("check info submit properly stores the users city", () => {});

  test("check info submit properly stores the users state", () => {});

  test("check info submit properly stores the users zipCode", () => {});
});
