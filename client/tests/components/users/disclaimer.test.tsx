import { render, screen, waitFor } from "@testing-library/react";

describe("testing user disclaimer component", () => {
  beforeEach(async () => {});
  test("check that the disclaimer modal appears on user age load", () => {});

  test("check that the disclaimer modal does not disappears upon clicking outside of modal", () => {});

  test("check that the disclaimer modal does disappears upon clicking 'i accept' in modal", () => {});

  test("check that the disclaimer modal does not appears upon admin page load", () => {});
});
