import { render, screen, waitFor } from "@testing-library/react";

describe("testing user component", () => {
  beforeEach(async () => {});
  test("check that the users menu is displayed upon being clicked", () => {});

  test("check that the users menu disappears upon clicking away", () => {});

  test("check that the users menu profile link directs you to the profile page", () => {});

  test("check that the users menu purchases link directs you to the purchases page", () => {});

  test("check that the users menu wishlist link directs you to the wishlist page", () => {});

  test("check that the users menu purchase history link directs you to the purchase history page", () => {});

  test("check that the users menu logout link logs you out", () => {});
});
