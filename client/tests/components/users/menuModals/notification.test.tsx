import { render, screen, waitFor } from "@testing-library/react";

describe("testing user notification component", () => {
  beforeEach(async () => {});
  test("check that the notifications menu is displayed upon being clicked", () => {});

  test("check that the notifications menu disappears upon clicking away", () => {});

  test("check that by default all notifications are displayed", () => {});

  test("check that the notifications menu all link displays all notifications", () => {});

  test("check that the notifications menu store link displays only store links notifications", () => {});

  test("check that the notifications menu courses link displays only courses notifications", () => {});

  test("check that the notifications menu media link displays only media notifications", () => {});

  test("check that the notifications menu events link displays only events notifications", () => {});
});
