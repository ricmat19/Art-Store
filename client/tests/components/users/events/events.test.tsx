import { render, screen, waitFor } from "@testing-library/react";

describe("testing user events component", () => {
  beforeEach(async () => {});
  test("check that table title row is displayed", () => {});

  test("check that event titles are displayed", () => {});

  test("check that event prices are displayed", () => {});

  test("check that event spots are displayed", () => {});

  test("check that event prices are displayed", () => {});
});
