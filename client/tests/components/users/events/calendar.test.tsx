import { render, screen, waitFor } from "@testing-library/react";

describe("testing user calendar component", () => {
  beforeEach(async () => {});
  test("check that the current month is displayed in the calendar title on render", () => {});

  test("check that the current year is displayed in the calendar title on render", () => {});

  test("check that the back button sends you a month back", () => {});

  test("check that the forward button sends you a month forward", () => {});

  test("check that the week days are properly rendered", () => {});

  test("check that month's days are properly rendered", () => {});

  test("check that the current day's background color is changed", () => {});

  test("check that hovering over a day changes the background color", () => {});
});
