import { render, screen, waitFor } from "@testing-library/react";

describe("testing user day component", () => {
  beforeEach(async () => {});
  test("check that the selected date is displayed as the title in the day modal", () => {});

  test("check that the selected days events are displayed in the modal", () => {});
});
