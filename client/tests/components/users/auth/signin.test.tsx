import { render, screen, waitFor } from "@testing-library/react";

describe("testing user signin component", () => {
  beforeEach(async () => {});
  test("check for validation alert if no email is provided", () => {});

  test("check for validation alert if invalid email address is provided", () => {});

  test("check for validation alert if no password is provided", () => {});

  test("check for validation alert invalid email address is provided", () => {});

  test("check that signin works if valid login credentials are provided", () => {});

  test("check that signin fails if invalid login credentials are provided", () => {});

  test("check that forgot password sends you to the forgot password modal", () => {});

  test("check that create account sends you to the create account modal", () => {});
});
