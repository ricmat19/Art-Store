import { render, screen, waitFor } from "@testing-library/react";

describe("testing user signup component", () => {
  beforeEach(async () => {});
  test("check for validation alert if no first name is provided", () => {});

  test("check for validation alert if no last name is provided", () => {});

  test("check for validation alert if no email is provided", () => {});

  test("check for validation alert if invalid email is provided", () => {});

  test("check for validation alert if no password is provided", () => {});

  test("check for validation alert if invalid password is provided", () => {});

  test("check for validation alert if no re-typed password is provided", () => {});

  test("check for validation alert if re-typed password does not match", () => {});

  test("check that signin link sends you to signin modal", () => {});
});
