import { render, screen, waitFor } from "@testing-library/react";

describe("testing user cart products component", () => {
  beforeEach(async () => {});
  test("check that cart item can be deleted", () => {});

  test("check that cart item image is displayed", () => {});

  test("check that cart item price is displayed", () => {});
});
