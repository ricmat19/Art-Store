import { render, screen, waitFor } from "@testing-library/react";

describe("testing the user products index page", () => {
  beforeEach(async () => {});
  test("check that store categories are mapped", () => {});

  test("check that store product images are mapped", () => {});

  test("check that store product titles are mapped", () => {});

  test("check that store product prices are mapped", () => {});
});
